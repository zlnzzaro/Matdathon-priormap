# Priormap PRD

## 1. 문서 목적

Priormap의 MVP 제품 요구사항을 정의한다. 이 문서는 4시간 30분 안에 핵심 사용자 경험을 구현하고, 해커톤 평가 기준에 맞는 데모를 완성하는 것을 목표로 한다.

## 2. 제품 개요

Priormap은 사용자가 여러 할 일을 자연어로 입력하면 AI가 task를 추출하고, 마감일·영향도·예상 소요 시간을 바탕으로 우선순위를 분석하는 개인 생산성 도구다.

AI는 가장 먼저 처리할 작업 1개를 `Focus Now`로 추천하고, 나머지 task는 아이젠하워 매트릭스에 배치한다. 사용자는 AI의 결과를 승인하기 전에 블록을 이동하거나 상세정보를 수정할 수 있다.

핵심 가치는 할 일을 기록하는 것이 아니라, 사용자가 지금 무엇부터 할지 결정하는 시간을 줄이는 것이다.

## 3. 대상 사용자

하루에 여러 업무와 요청을 동시에 처리해야 하는 초기 스타트업 실무자 또는 해커톤 참가자.

이 사용자는 회의, 메신저, 문서에서 생긴 할 일을 한꺼번에 적지만 마감일과 업무 영향도를 비교하기 어렵다. 그 결과 계획을 세우는 데 시간이 걸리고, 중요한 작업보다 쉬운 작업부터 처리하기 쉽다.

## 4. 문제 정의

기존 Todo 앱은 할 일을 기록하는 데는 도움을 주지만, 여러 task 중 지금 먼저 해야 할 일을 결정하는 과정은 충분히 지원하지 못한다.

Priormap은 자연어 입력을 분석해 우선순위 결정을 보조하고, 추천 이유를 제시하되 최종 판단은 사용자에게 남긴다.

## 5. 목표와 비목표

### 목표

- 여러 할 일을 한 번에 자연어로 입력할 수 있다.
- AI가 입력에서 여러 task를 추출할 수 있다.
- AI가 중요도·긴급도·예상 소요 시간을 분석할 수 있다.
- 가장 먼저 처리할 task 1개를 Focus Now로 추천할 수 있다.
- 추천 이유와 추정값 여부를 표시할 수 있다.
- 사용자가 매트릭스 영역 간 드래그 앤 드롭으로 task를 이동할 수 있다.
- 사용자가 task 상세정보를 수정할 수 있다.
- 사용자의 수정 및 승인 없이는 최종 상태를 확정하지 않는다.

### 비목표

- 팀 협업, 공유, 댓글
- 캘린더·이메일·메신저 연동
- 로그인과 사용자별 장기 저장
- 알림과 반복 일정
- 멀티 에이전트 오케스트레이션
- 블록 내부 정렬, 다중 선택 등 복잡한 드래그 앤 드롭
- 사용자 승인 없는 자동 실행

## 6. 핵심 사용자 흐름

1. 사용자가 메인 화면의 입력창에 여러 할 일을 자연어로 입력한다.
2. 사용자가 `우선순위 분석하기`를 선택한다.
3. AI가 task 추출, 우선순위 분석, Focus Now 계산, 매트릭스 배치를 진행한다.
4. 분석 상태를 스트리밍 메시지로 보여준다.
5. Focus Now 작업 1개와 추천 이유를 보여주고, 나머지 task를 매트릭스에 배치한다.
6. 사용자가 블록을 다른 영역으로 이동하거나 블록을 클릭해 상세정보를 수정한다.
7. 사용자가 승인하면 최종 우선순위와 매트릭스 위치를 확정한다.

## 7. 입력 경험

첫 화면은 입력 중심으로 구성한다.

- 자연어 입력창 1개
- 입력 예시와 쉼표 구분 안내
- `우선순위 분석하기` 버튼

입력 안내 예시:

> 해야 할 일을 쉼표로 구분해 입력해 주세요.
> 예: 발표 자료 작성, 팀원에게 회의 일정 보내기, 보고서 초안 작성

MVP에서는 쉼표로 구분한 입력을 기본으로 지원한다. 쉼표가 포함된 자연스러운 문장도 모델이 task 경계를 해석할 수 있도록 허용하되, 분석 결과에서 추출된 task를 사용자가 수정할 수 있어야 한다.

## 8. 분석 중 화면

분석 중에는 단순 스피너 대신 현재 작업 단계를 표시한다.

- `할 일을 추출하고 있습니다...`
- `마감일과 영향도를 분석하고 있습니다...`
- `Focus Now를 선정하고 있습니다...`
- `매트릭스에 배치하고 있습니다...`

분석 실패 시 입력 화면으로 돌아갈 수 있는 재시도 동작을 제공한다.

## 9. 결과 화면

### Focus Now 영역

결과 화면 상단에 다음 정보를 표시한다.

- 추천 task 제목
- 마감일
- 예상 소요 시간
- `AI 추천` 라벨
- 추천 이유
- 승인 또는 수정 동작

Focus Now는 중요도와 긴급도만으로 결정하지 않고 마감일·영향도·예상 소요 시간 등 분석 가능한 정보를 종합해 1개를 추천한다.

### 아이젠하워 매트릭스

다음 네 영역을 제공한다.

- 중요하고 긴급함
- 중요하지만 긴급하지 않음
- 중요하지 않지만 긴급함
- 중요하지도 긴급하지도 않음

AI가 처음 배치한 task 블록은 사용자가 다른 영역으로 드래그 앤 드롭할 수 있다. MVP에서는 영역 간 이동만 지원한다.

## 10. task 상세 편집

블록을 클릭하면 상세 편집 패널 또는 모달을 연다.

편집 가능한 항목:

- 제목
- 상세 내용
- 마감일
- 예상 소요 시간
- 중요도
- 긴급도

사용자가 직접 이동하거나 수정한 값은 AI의 초기 추천보다 우선한다. AI를 다시 실행하더라도 사용자 수정값을 임의로 되돌리지 않는다.

## 11. 신뢰성과 책임 있는 AI

- AI가 만든 task와 추천에는 `AI 추천` 라벨을 표시한다.
- 추천 이유를 함께 표시한다.
- 입력에 근거가 없는 마감일·영향도·소요 시간은 추정값으로 표시한다.
- 사용자의 승인 전에는 결과를 최종 확정하지 않는다.
- 모델 응답이 불완전하거나 분석에 실패하면 사용자가 재시도할 수 있다.
- 민감한 입력을 저장하거나 로그에 원문 그대로 남기지 않는다.

## 12. 수용 기준

### 핵심 기능

- [ ] 입력한 여러 할 일이 task 카드로 추출된다.
- [ ] 각 task에 중요도와 긴급도가 표시된다.
- [ ] Focus Now가 정확히 1개 표시된다.
- [ ] Focus Now의 추천 이유가 표시된다.
- [ ] 나머지 task가 네 매트릭스 영역 중 하나에 배치된다.
- [ ] task 블록을 다른 매트릭스 영역으로 이동할 수 있다.
- [ ] task 블록을 클릭해 상세정보를 수정할 수 있다.
- [ ] 사용자가 승인한 뒤 수정 결과가 유지된다.

### 상태와 오류

- [ ] 빈 입력을 제출할 수 없다.
- [ ] 분석 중 상태가 표시된다.
- [ ] 분석 실패 시 오류 메시지와 재시도가 제공된다.
- [ ] 예상치 못한 AI 응답이 화면을 깨뜨리지 않는다.
- [ ] 사용자가 수정한 위치가 자동 재분석으로 덮어써지지 않는다.

## 13. 성공 신호

- 회의와 메신저에서 나온 6개의 할 일을 한 번에 입력할 수 있다.
- 사용자가 분석 결과를 확인하고 약 10초 안에 첫 작업을 결정할 수 있다.
- 사용자가 추천 이유를 이해하고 승인 또는 수정할 수 있다.
- 사용자가 AI 추천과 자신의 수정 결과를 구분할 수 있다.

## 14. 해커톤 평가 기준 대응

- Copilot SDK와 Microsoft Agent Framework를 모델 연결, 에이전트 오케스트레이션, 도구 호출, 컨텍스트 처리, 스트리밍에 사용한다.
- 개인 생산성 문제를 Focus Now 추천과 결정 시간 단축으로 해결한다.
- Azure에 배포하고 비밀 관리와 기본 관찰 가능성을 확보한다.
- 입력부터 승인까지 끝까지 동작하는 단일 핵심 흐름을 제공한다.
- AI 추천 근거와 사용자 수정 권한을 표시해 신뢰성을 확보한다.

## 15. 데모 시나리오

사용자가 다음과 같이 6개의 업무를 입력한다.

> 내일 발표 자료 완성, 팀원에게 회의 일정 보내기, 고객 문의 답변, 보고서 초안 작성, 장보기, 개발 환경 오류 조사

AI는 각 task를 추출하고 매트릭스에 배치한 뒤, 마감일과 영향도가 높은 `내일 발표 자료 완성`을 Focus Now로 추천한다. 사용자는 카드의 상세정보를 확인하고, 필요하면 다른 영역으로 이동한 뒤 승인한다.
